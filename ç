#ifndef CEDILLE_H
# define CEDILLE_H
# define macro	
# define tcopy2(x, y) x##y
# define tcopy(x, y) tcopy2(c, y)
# define rule	char *
// tcopy(rule, __LINE__) 

macro rule test42(const char *s, size_t l)
{
	return (0);
}
#endif
